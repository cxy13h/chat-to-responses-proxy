{
    "name": "chat-to-responses-proxy",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy --keep-vars"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}